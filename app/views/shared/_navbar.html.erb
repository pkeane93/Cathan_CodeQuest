<div class="w-full px-15 border-b border-gray-600 bg-gray-900 opacity-80 flex justify-between fixed z-2">

  <%# Navbar Start  %>
  <div class="flex gap-15">
    <figure class="h-20">
      <%= link_to(posts_path)do%>
        <%= image_tag( "Cathan_logo_03_bg_black.png", class: "h-full w-full object-cover" ) %>
      <%end%>
    </figure>

    <div class="items-center flex gap-6 font-semibold">
      <%= link_to("ARTICLES", posts_path)%>
      <%= link_to("ABOUT", about_path)%>
    </div>
  </div>

  <%# Navbar end  %>
  <div class="items-center flex gap-6 font-semibold">
    <% if policy(Post.new).new? %>
      <%= link_to "LOGOUT", destroy_user_session_path, data: { turbo_method: :delete } %>
    <% end %>
  </div>

</div>
